package dailydairy;

import java.util.Scanner;

public class FactorsOfaGivenNumber {
	public static void main(String []args)
	{
	  Scanner sc=new Scanner(System.in);
      int n=sc.nextInt();
       int v=factorsOfaNum(n);
     //System.out.print(v);
	}
	static int factorsOfaNum(int k)
	{
        for(int i=1;i<=k;i++) // if we give i=0 the output will be error
        {
        	if(k%i==0)
        	{
        		System.out.print(i+" "); 
        		//return i; //  return value in the loop  will terminate the loop  if it satisfies the condition and prints the output ,it will not do further iterations
        	}
        }
        return -1;
}
}